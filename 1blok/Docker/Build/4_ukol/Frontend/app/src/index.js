import React from 'react';
import ReactDOM from 'react-dom';

const element= <h1>Hello world from react webapp running inside frontend conteiner!</h1>;

///console.log(element);

ReactDOM.render(element,document.getElementById('root'));